{
  "cells": [
    {
      "id": "md_intro",
      "type": "markdown",
      "content": "# PropertyGrid Reactivity Test\n\nSimple test to verify that PropertyGrid editing propagates to other cells."
    },
    {
      "id": "code_config",
      "type": "code",
      "code": "// Create a simple configuration object\nexports.config = {\n  appName: \"Test App\",\n  port: 3000,\n  debug: true\n};\n\noutput(exports.config);"
    },
    {
      "id": "md_instructions",
      "type": "markdown",
      "content": "## Instructions\n\n1. **Edit the config above**: Try changing the appName, port, or debug flag\n2. **Run the cell below** to see if changes propagate\n\n**Expected behavior**:\n- PropertyGrid should show editable fields with pencil icons on hover\n- Clicking anywhere on a field should allow editing\n- Changes should be reflected in the verification cell below"
    },
    {
      "id": "code_verify",
      "type": "code",
      "code": "// Verify that changes propagate\nconsole.log('Current config values:');\nconsole.log('- App Name:', config.appName);\nconsole.log('- Port:', config.port);\nconsole.log('- Debug:', config.debug);\n\n// Create a computed value based on config\nconst serverUrl = `http://localhost:${config.port}`;\nconst status = config.debug ? 'Development' : 'Production';\n\nexports.computed = {\n  serverUrl,\n  status,\n  fullTitle: `${config.appName} (${status})`\n};\n\noutput(exports.computed);"
    },
    {
      "id": "md_result",
      "type": "markdown",
      "content": "## Test Results\n\nIf editing works correctly:\n- Changes to `config.port` should update the `serverUrl`\n- Changes to `config.debug` should toggle between 'Development' and 'Production'\n- Changes to `config.appName` should update the `fullTitle`\n\n**UI Improvements**:\n- ✅ Pencil icon instead of \"Editable\" badge\n- ✅ Click anywhere on field to edit (not just pencil icon)\n- ✅ PropertyGrid clicks don't select the notebook cell\n- ✅ Changes propagate to dependent cells"
    }
  ]
}
