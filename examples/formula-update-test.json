{
  "cells": [
    {
      "type": "input",
      "id": "input-base",
      "variableName": "basePrice",
      "value": 100,
      "label": "Base Price"
    },
    {
      "type": "input", 
      "id": "input-tax",
      "variableName": "taxRate",
      "value": 0.08,
      "label": "Tax Rate"
    },
    {
      "type": "formula",
      "id": "formula-total",
      "variableName": "totalPrice",
      "formula": "basePrice * (1 + taxRate)"
    },
    {
      "type": "markdown",
      "id": "display-result",
      "content": "**Final Total:** ${{totalPrice?.toFixed(2) || 'Calculating...'}}\n\n**Test Instructions:**\n1. Edit the formula above - change it to `basePrice * 2` \n2. **Important:** Click \"Apply Changes\" or press Ctrl+Enter to commit\n3. Then change the basePrice input value\n4. The result should reflect the NEW formula (basePrice * 2), not the old one\n\n✅ **Expected behavior:** Formula only updates after explicit commit\n❌ **Previous bug:** Formula used old definition even after editing"
    }
  ]
}
